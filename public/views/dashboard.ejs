<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Account Created - Dashboard</title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        .dashboard-container {
            text-align: center;
        }
        .success-icon {
            width: 80px;
            height: 80px;
            margin: 0 auto 30px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            animation: scaleIn 0.5s ease-out;
        }
        .success-icon::before {
            content: '✓';
            color: white;
            font-size: 50px;
            font-weight: bold;
        }
        @keyframes scaleIn {
            from {
                transform: scale(0);
                opacity: 0;
            }
            to {
                transform: scale(1);
                opacity: 1;
            }
        }
        .success-message {
            font-size: 24px;
            color: #333;
            font-weight: 600;
            margin-bottom: 15px;
        }
        .success-description {
            font-size: 16px;
            color: #666;
            margin-bottom: 30px;
        }
    </style>
</head>
<body>
    <div class="container dashboard-container">
        <div class="success-icon"></div>
        <h1 class="success-message">Account Created Successfully!</h1>
        <p class="success-description">Your account has been created and saved to the database.</p>
        <div class="login-link">
            <a href="/api/v1/user/login">Go Back to Login</a>
        </div>
    </div>
</body>
</html>

